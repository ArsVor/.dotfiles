local ibl = require('ibl')

ibl.setup({
  indent = {
    char = '▏',
  },
  scope = {
    char = '▎',
    enabled = true,
    show_start = false,
    show_end = false,
    show_exact_scope = false,
  },

})
